[ ] create a frontend page with a markdown editor to populate wiki articles
    -include text, photos, links, and code
    -include tags

in the same way that the wiki editor is local only, can we extend the repo to include repos but only in local mode?
